import 'package:flutter/material.dart';

class StepperSuccess extends StatelessWidget {
  const StepperSuccess({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: EdgeInsets.symmetric(
        horizontal: 10,
        vertical: 10,
      ),
      child: Container(),
    );
  }
}
